# MySQL. Versions 5.1.10 and up are supported.
#
# Install the MySQL driver
#   gem install mysql2
#
# Ensure the MySQL gem is defined in your Gemfile
#   gem 'mysql2'
#
# And be sure to use new-style password hashing:
#   https://dev.mysql.com/doc/refman/5.7/en/password-hashing.html
#
default: &default
  adapter: mysql2
  encoding: utf8
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: root
  password:
  socket: /tmp/mysql.sock

development:
  <<: *default
  database: jirodo_development
  host: localhost

test:
  <<: *default
  database: jirodo_test


production:
  <<: *default
  database: heroku_254e6ad0b22dd3f
  username: bab5d197e7bda1
  password: <%= ENV['JIRODO_DATABASE_PASSWORD'] %>

